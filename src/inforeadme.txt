//to install eslint
yarn add -D eslint-plugin-jest-dom

//to install prettier
yarn add --dev --exact prettier

//to run prettier and eslint run on same project without conflict
yarn add -D eslint-config-prettier

//to install husky
npx husky-init && yarn
//incase of npm
npx husky-init && npm

//install lint-staged
yarn add -D lint-staged